@page
@model Test3.Pages.Admin.CategoryAdmin.DetailsModel

@{
    ViewData["Title"] = "Details";
}

<h1>Detaljer</h1>

<div>
    <h4>Kategori</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Category.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Category.Name)
        </dd>
    </dl>
</div>
<div>
    <a  asp-page="./Edit" asp-route-id="@Model.Category.Id" class="btn btn-primary">Redigera</a> 
    <a asp-page="./Index" class="btn btn-secondary">Tillbaka till listan</a>
</div>
